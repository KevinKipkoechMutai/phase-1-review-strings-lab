// Write your code in this file!
//definition of the currentUser variable and assigning it to a string
const currentUser = "Kevin";

//introducing the welcomeMessage variable
let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

//introducing the excitedWelcomeMessage variable
let excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

//introducing the shortGreeting variable
const firstInitial = currentUser[0];
let shortGreeting = `Welcome, ${firstInitial}!`;